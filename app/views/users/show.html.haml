.contents.row
  .breadcrumb
    - breadcrumb :mypage
  %p
    = @nickname
    さんの投稿一覧
  - @tweets.each do |tweet|
    = render partial: "tweets/tweet", locals: { tweet: tweet }
  = paginate(@tweets)

